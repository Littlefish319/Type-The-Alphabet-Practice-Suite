import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yunova.alphabettypingsuite',
  appName: 'AlphaTyper',
  webDir: 'dist'
};

export default config;
